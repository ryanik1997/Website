import './validate-cam-reading.mts'

import { describe, it, expect } from 'vitest'

describe('Cambridge Reading validation runner', () => {
  it('loads the validator runner', () => {
    expect(true).toBe(true)
  })
})
