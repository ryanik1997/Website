# Known issues

## IELTS Reading catalog

- `reading-cam-11-2.json` is excluded from the builtin catalog seed because the
  converted payload contains 41 questions instead of the expected 40. Review
  the source question grouping manually before importing it.
