# 2026-07-28

- Đã đánh giá sơ bộ ba repo skill `DietrichGebert/ponytail`, `mattpocock/skills`, và `ayghri/i-have-adhd`. Cả ba tương thích ở cấp Agent Skills/SKILL.md; plugin/hook và metadata riêng từng host có thể cần bỏ hoặc chuyển đổi. Chưa cài đặt gì; khuyến nghị audit bảo mật, chọn lọc skill và pin commit/tag trước khi cài.
- Task 4 FCE B2 Reading Part 7: đã thêm parser DOM label/heading/body, field `heading` xuyên package/runtime/media repair/renderer, audit 26 tests (110 sections; 93 có source heading, 17 source không có heading riêng), rebuild catalog và validators package/runtime PASS. Targeted UI tests/typecheck PASS; browser smoke 5 đề còn bị chặn vì dev auth-bypass không vào được DOM exam trong timeout, chưa có screenshots/commit.
- Task 4 browser smoke đã được gỡ blocker bằng `VITE_DEV_AUTH_BYPASS=1` và selector footer khớp `Part 7` kèm count; App Tests 2/5/14/20/27 PASS 5/5 và đã chụp 5 screenshots. Final validators/catalog/typecheck vẫn PASS; full exam suite còn đúng 1 lỗi IELTS count ngoài phạm vi (47 expected, 48 generated). Không bịa heading cho 17 section raw source không có heading riêng.
- Task 5 Cambridge Writing AI: đã triển khai pipeline B1-C2 gồm provenance optional trong schema chuẩn, level config có `NEW_TESTS_PER_LEVEL`, plan 200 rows, adapter AI độc lập generation/verification, staging/revision/quarantine, validator/similarity/index/promote/report và package scripts. Chưa sinh corpus vì thiếu cả generation/verification API key; trạng thái đúng là CHƯA HOÀN THÀNH (0 tests/0 tasks). Pipeline tests 5/5, Writing 33/33, DB 6/6 và TypeScript PASS; cần cấu hình key rồi chạy checkpoint 02-06 trước.
- Đã xác nhận cấu hình thực tế của Task 5: DeepSeek `deepseek-chat` dùng để sinh, Groq `llama-3.3-70b-versatile` dùng để kiểm định độc lập; cả hai key đều đã được nạp từ file local bị git-ignore. Ghi chú trước đó nói thiếu key đã lỗi thời; bước tiếp theo là chạy checkpoint 02-06.
