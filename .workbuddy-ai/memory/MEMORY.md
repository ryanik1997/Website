# Project Memory

- Cambridge Writing AI corpus pipeline uses DeepSeek (`deepseek-chat`) for generation and Groq (`llama-3.3-70b-versatile`) for independent verification. Credentials are loaded from ignored `.env.cambridge-writing.local`; never expose or commit keys.
