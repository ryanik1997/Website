# 2026-07-31

- Hoàn thành Task 8: sửa PET B1 Reading Tests 46–50 Part 4. Thêm canonical blueprints dạng `paragraphs[].segments[]`, mỗi đoạn đúng một gap Q16–Q20; compiler kiểm tra schema, deterministic shuffle option bank theo exam id và derive answer vault từ `correctForGap`.
- Regenerated package/public/vault chỉ cho Part 4 Tests 46–50. Contract PASS: 304–325 words, 5 đoạn 55–75 words, 8 options/3 distractors, answers không monotonic, package/public parity.
- Browser DOM evidence PASS 5/5, không screenshot: mỗi route có title, 5 `.pet-rw-part4-paragraph`, gap order 16–20, 8 bank cards, không page errors. PET validator PASS 38 tests/1216 questions; renderer Part 4 vitest PASS 9/9.
- Xác nhận Task 8 đã được push: local `main` và `origin/main` cùng ở `e9d3afde92c58696244ede4726b700139a045ee8`; commit Task 8 `92d766b0` có trong lịch sử remote.
- Audit Crawl4AI v0.9.2 commit `7e801521428ee12509994d39151006f64055ebe3` và tạo user-level adapter skill tại `C:/Users/lindv/.workbuddy-ai/skills/crawl4ai`. Adapter không bundle/chạy mã upstream; ghi rõ URL/SSRF, JS, browser-profile, plaintext-token, cache deletion, browser/model download và prompt-injection guardrails. Skill validator PASS và load thử thành công.
- Task 10 CAE EngExam: hoàn tất schema discovery nhưng dừng bulk crawl/import. EngExam Terms mục 5 cấm unauthorized use/reproduction/redistribution; robots.txt không disallow path nhưng không cấp license. Không tải audio, không tạo Tests 2–30. Cần written permission hoặc corpus được cấp phép. Crawl4AI 0.9.2 đã được cài wheel-only trong managed venv riêng, không chạy browser/model setup; skill được bổ sung bắt buộc kiểm tra ToS cho bulk import/republication.
- Hoàn thành Task 11, không commit/push: xác nhận không giữ raw EngExam content/audio; tạo offline licensed CAE Listening scaffold tại `tmp/cae-listening-licensed-input/` và converter/validator/tests trong `scripts/listening/cae-c1/`. Pipeline fail-closed với metadata quyền sở hữu/license, bắt buộc written permission cho `engexam.info`, kiểm tra folder/testNumber, 4 part/30 câu, answers/transcripts, local non-empty MP3, path traversal, answer stripping/vault parity, atomic apply, conflict refusal và idempotency. Synthetic suite, empty-root validation/dry-run, catalog runtime, web TypeScript và `git diff --check` đều PASS; browser validation chờ corpus hợp pháp thật.
- Audit yêu cầu cài user-level skill từ `D:/mcps/ai-website-cloner-template-master/ai-website-cloner-template-master`: repo là Next.js app/template, không phải một skill package duy nhất; chứa bốn bản `clone-website/SKILL.md` giống hệt SHA-256 `65305b74...`. Audit xếp P1 vì workflow mặc định sao chép/download toàn bộ nội dung, ảnh, video, font từ website, có thể thao tác site đăng nhập, tự tạo script tải tài sản, chạy build và merge worktree mà chưa có guardrails về quyền nội dung, bí mật/session, URL/SSRF và xác nhận destructive/external actions. Chưa cài; chờ user xác nhận cài bản adapter đã harden, chỉ lấy SKILL.md + MIT attribution, không copy toàn repo/build artifacts/research data.
- Hoàn thành Task 13 `/app/sentence-structure`: toolbar responsive với filter kết hợp, saved card và `?view=saved`, 3 learning statuses persist trong SentenceStructure Dexie hiện có, legacy mặc định `not_started`, accessible status menu và error rollback. Browser QA PASS 19 checks, console 0; targeted 4/4, DB 6/6, tsc và diff-check PASS. Full web suite có 413 pass/2 fail ngoài phạm vi cùng 6 worker exits. Screenshots ở `outputs/task13-*.png`.
- Hoàn thành Task 14 Focus Mode cho `/app/sentence-structure/catalog:*`: mở rộng full-bleed predicate tập trung trong AppShell để ẩn sidebar/mobile drawer/global widgets; Focus Header route riêng dùng history Back/fallback list, title/CEFR, tiến độ 1/1 và Thoát; workspace 60rem responsive/100dvh; feedback có aria-live. Browser QA PASS 28 checks tại 1920×1080, 1366×768, 390×844, query Back/Forward giữ nguyên, console errors/warnings 0; targeted 77/77, TypeScript và diff-check PASS. Screenshots ở `outputs/task14-focus-*.png`.
